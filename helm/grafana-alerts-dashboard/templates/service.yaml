apiVersion: v1
kind: Service
metadata:
  name: {{ include "grafana-alerts-dashboard.fullname" . }}
  labels:
    {{- include "grafana-alerts-dashboard.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "grafana-alerts-dashboard.selectorLabels" . | nindent 4 }}
